#!/bin/bash
# Starts the dev environment: Tailwind CSS watcher, the Vite dev server (the
# browser-facing app on port 5173), and the Express OAuth server (port 3000).
# `npm run dev` runs all three concurrently.
set -e

cd "$(dirname "$0")/../.."

echo "Starting development server"
npm run dev
